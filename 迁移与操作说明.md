# 迁移与操作说明

## 新增：更方便的统一窗口与扫描进度

现在可以直接使用根目录的`打开统一工作台.vbs`，在同一窗口中切换数据准备、训练、单信道预测和批量预测。训练前扫描、训练批次和拟合评估均显示数量、百分比及耗时，日志保存于`outputs/ui_logs`。

已有离线Python环境时，在`python_path.txt`中填写该环境`python.exe`完整路径即可启动；也可以进入窗口后在顶部选择任务使用的解释器。留空时仍查找项目内`.venv`。不需要运行联网安装CMD。原编号CMD保留作为命令行方式，下面的安装步骤只适用于确实需要新建环境的电脑。

详见`docs/统一工作台使用说明.md`。本版仍不含旧数据/旧权重，默认原始主径501。

## 1. 这份包的用途与包含内容

用于在另一台Windows电脑上，使用那台电脑的数据重新训练神经网络。原始信道主径按MATLAB第501列处理，变量名仍为`h_vary`，监督目标为MeanCodedBER（译码前BER）。

包含核心网络、MAT/NPZ适配器、通用数据准备脚本、训练脚本、预测窗口、绘图脚本、软件测试、配置模板和说明。没有旧训练数据、旧参数表、旧结果或旧权重。`data`和`outputs`将在新电脑执行相应步骤时生成内容。

不需要安装Codex，也不依赖本机技能目录。神经网络训练和预测不需要MATLAB；新信道BER比较仍需你自己的MATLAB接收机程序。

## 2. 新电脑安装环境

推荐64位Python 3.12，安装时保留Tcl/Tk组件（预测窗口需要）。不要复制旧电脑的Python虚拟环境，应该在新电脑重新创建。

在移植包根目录打开PowerShell：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
```

### 只安装CPU版本

可以双击`01_安装CPU环境.cmd`，或执行：

```powershell
.\.venv\Scripts\python.exe -m pip install torch --index-url https://download.pytorch.org/whl/cpu
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

CPU可训练和预测，但大数据训练可能很慢。这个安装脚本明确安装CPU版，不会自动安装GPU版。

### 使用NVIDIA显卡训练

先在[PyTorch官方安装页](https://pytorch.org/get-started/locally/)选择Windows、Pip、适合显卡与驱动的CUDA版本，将官方命令中的`pip`替换为`.\.venv\Scripts\python.exe -m pip`。

然后安装剩余依赖：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
```

计划使用GPU时，最后一项应该为`True`。如果之前安装过CPU版，需要按官方命令切换为CUDA构建；不要在GPU安装后再运行CPU安装脚本。默认`training.device=auto`，没有可用CUDA时程序会使用CPU，不会自动安装驱动。

本机代码曾在Python3.12、PyTorch2.11运行；最低依赖版本不代表已经逐一验证过所有版本与所有GPU。迁移包已做独立软件自检，新电脑也应执行`05_软件自检.cmd`。

## 3. 填写新电脑的数据位置

用记事本或代码编辑器打开`configs/dataset.json`。只要新数据保存结构与原项目相同，首先修改这两项：

```json
"channel_dir": "E:/你的原始信道目录",
"result_dir": "E:/你的网格结果目录"
```

这些只是示例路径，要换成新电脑的实际位置。JSON路径建议用`/`；若使用反斜杠，需要写成`\\`。不要在JSON中加注释、尾逗号或中文引号。

- `channel_dir`：包含完整动态`h_vary`的原始MAT目录。
- `result_dir`：包含每个信道294组参数网格结果的MAT目录。
- `result_pattern`：默认`grid_*.mat`。若文件不带grid前缀可改为`*.mat`，但要避免把汇总MAT和无关MAT一起纳入。
- `output_dir`：默认`data/prepared`，存小体积标签和索引，不复制完整原始信道。
- `expected_candidates`：默认294。若新数据的网格不同，要与真实数量一致，不能随意补齐。

程序递归扫描结果目录，通过结果中的`currName`找到原始信道。先查信道根目录；若没有再查子目录，同名文件不唯一时会停止。不会沿用`currName`内可能残留的旧电脑绝对路径。

## 4. 数据字段与配对约定

默认读取：

| 内容 | 字段名 | 预期含义 |
|---|---|---|
| 完整动态信道 | h_vary | 时间×时延复数矩阵 |
| 原始信道文件名 | currName | 对应的信道MAT文件名 |
| 信噪比 | currSNR | 单个有限dB标量 |
| 步长候选 | param_delt | 每个候选的mu |
| 前馈抽头候选 | param_N1 | 与mu逐项对应 |
| 反馈抽头候选 | param_N2 | 与mu逐项对应 |
| 译码前MC误码率 | coded_ber_after_mc | MATLAB逻辑尺寸：候选数×重复次数 |

字段名不同可改`fields`。传统MAT结构可以使用点路径，例如`result.currSNR`。数值v7.3 MAT默认恢复MATLAB维序，普通HDF5若已按逻辑维序保存可关闭`hdf5_matlab_order`。

标签等于`coded_ber_after_mc`每一行跨重复次数的平均，不使用Decoded字段。MC次数从实际数组获取，不写死25；样本数从扫描文件获取，不写死110。单次MC的传统MAT向量也支持。

相邻同名CSV存在时，程序按`ParamIndex`恢复顺序，核对其中的`MeanCodedBER`。没有CSV时使用MAT中的MC结果，并在审计报告记录实际核对了多少份CSV。

这不等于支持任意MAT布局：v7.3 cell/对象引用、多信道共存一个结果文件、只有最佳参数没有完整代价、只有CSV没有MC结果等，需要进一步修改`scripts/prepare_data.py`。缺失标签、越界BER、参数表不一致时会报错，不把缺失数据补成零。

## 5. 主径与裁剪设置

`configs/train.json`中默认：

```json
"crop": {
  "already_cropped": false,
  "time_start_matlab": 626,
  "time_stop_matlab": 4000,
  "main_path_matlab": 501,
  "delay_before": 250,
  "delay_after": 250,
  "allow_short": false
}
```

对应MATLAB：`h_vary(626:4000,251:751)`，Python：`h[625:4000,250:751]`。尺寸3375×501，裁后主径第251列。时间索引继续遵从用户指定，不能仅凭文件名确认物理秒数。前后250点对应两秒的前提是时延采样率125Hz。

原始数据至少覆盖4000行和751列；不足时默认报错。确认要接纳短时间记录时，可以设置`allow_short=true`，只用626至实际末尾，不补造数据。时延越界仍报错。

文件如果已经是裁好的时间×501矩阵，可设置`already_cropped=true`；这时不再处理原始时间和主径索引，保留文件实际时间长度。不要混用已裁剪和未裁剪的文件。

新数据的时间/时延采样率应在接入前确认；只把主径位置改为501，不能自动处理不同采样率。仿真的SNR定义、编码、调制、训练长度应在`simulation`配置中记录为新数据的实际条件。

## 6. 准备训练索引

双击`02_检查并准备数据.cmd`。

程序逐个读取并检查原始信道与结果，生成：

```text
data/prepared/
  candidates.csv      从新数据提取的真实候选表
  index.csv           新样本索引，全部split=train
  channel_*.npz       各样本的MeanCodedBER和候选编号
  provenance.json     原始路径、矩阵尺寸、MC次数
  audit.json          数量、标签目标、CSV核对数、裁剪设置
```

原始信道仍在你填写的目录，不会复制到包里。标签路径相对于索引；原始信道使用在新电脑解析出的绝对路径。以后移动原始数据或再迁移电脑时，重新建立索引。

遇到错误会停止并写`FAILED.json`，不会静默丢掉错误样本。准备目录非空时拒绝覆盖；修正配置后请使用新的`output_dir`，或者确认旧目录确实只是失败准备产物后自行清理。

成功后脚本会同步训练配置中的索引、候选路径、信道字段、HDF5维序及候选数量。

## 7. 训练并输出图

双击`03_训练并出图.cmd`。默认4层Transformer、80轮、每批4个信道，全部新样本参与训练，从随机权重开始，不加载旧模型。

主要可改的`training`项：

| 设置 | 默认 | 意义 |
|---|---:|---|
| epochs | 80 | 训练轮次 |
| batch_size | 4 | 每批信道数，显存不足先减为2或1 |
| learning_rate | 0.0005 | 初始学习率 |
| device | auto | 自动选择CUDA或CPU |
| output | outputs/new_training | 每次训练独立输出目录 |
| mode | all_data | 当前脚本要求全量训练 |

默认关闭`cache_features`，避免大数据集全部特征占满内存。原始大MAT会重复读取；若新数据很大，训练前最好进一步做按需缓存或分片优化，不要把十万个完整信道直接全缓存到内存。当前没有声称验证过十万文件的训练吞吐。

输出目录非空时拒绝覆盖。重新训练请改为例如`outputs/run_002`。这个包没有额外重建模型配置的固定110脚本，直接修改`configs/train.json`即可生效。

每轮控制台显示`fit BER`，它是训练集拟合而不是验证集。第4轮加入组合损失，损失值可能跳变。固定训练到epochs指定的最后一轮保存`model.pt`，不是验证早停。

结果包含权重、配置、归一化、固定参数基线、逐轮CSV、最终训练指标、逐信道选参CSV，以及`figures/training_curves.png/pdf/svg`。曲线采用英文标题保证跨电脑字体可读，对应总损失、MeanCodedBER、Top1/Top3命中率、regret四个面板。

如果训练完成但绘图失败，权重仍保留。修正matplotlib问题后可执行：

```powershell
.\scripts\python.ps1 -m scripts.plot_training
```

## 8. 预测新信道

训练成功后，脚本自动将`configs/inference.json`指向新权重。双击`04_打开预测窗口.vbs`：选择MAT，填SNR，主径默认501，点击开始预测。

输出mu、N1、N2及前5候选，同时保存JSON和MAT。点击复制MATLAB参数，或者在MATLAB中load输出的MAT文件。新信道预测不需要它的BER标签。

未训练时打开窗口可以显示界面，但预测会提示尚无模型。窗口默认读取`h_vary`；若使用其他字段，应相应修改窗口输入。这里没有捆绑MATLAB均衡仿真：真实MeanCodedBER需用推荐参数在你的接收链中另算，再与固定参数公平比较。

VBS不能启动时，在根目录执行`.\.venv\Scripts\python.exe -m scripts.predict_gui`查看报错。

## 9. 检查与故障处理

- 找不到Python：确认64位Python3.12和项目内`.venv/Scripts/python.exe`存在。
- No module named：确认依赖装在`.venv`里，而不是别的Python。
- CUDA=False：CPU仍能运行；需要GPU时检查驱动和PyTorch构建。
- 找不到文件：检查dataset.json目录和结果中的currName；不要将结果MAT当原始信道。
- 裁剪越界：核实原始尺寸、主径、已裁剪开关，不要盲目补零。
- MeanCodedBER与CSV不一致：先确认MC轴和候选顺序，不能直接忽略核对。
- 显存不足：降低batch_size。进程中断后不会自动续训，需用新的空输出目录重新开始。
- 不能仅改层数后使用旧权重；改变网络结构需重训。
- 全量训练没有独立测试证据。未来外部信道应使用相同SNR/采样/接收链定义，并做独立误码仿真。

## 10. 迁移后哪些代码不必改

根目录由程序自动定位；启动脚本统一使用项目内`.venv`，不需要改成某个用户名的绝对路径。核心CNN、Transformer、损失和预测代码通常不需要改。只要新数据格式与上表一致，首先填写两个目录，核对501裁剪与训练设置即可。


新增“消融与多模型”工作台页面，支持并发训练、多种子、组外比较、曲线和最终BER对比。详细操作见 docs/消融与多模型工作台.md。
