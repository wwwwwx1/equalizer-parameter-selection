# MATLAB集成与配置编辑验证记录

## 本机实际执行

- MATLAB R2023b可启动，通信、信号处理和并行工具箱许可检查可用。
- 预测参数仿真：实际原始信道1个、MC=1，成功1个，生成v7.3 MAT。
- 完整网格仿真：同一信道、MC=1、完整294候选，成功1个，生成v7.3 MAT。
- 选择性保存：同一预测仿真，关闭Decoded MC/MSE/H/网格表，MAT v7、CSV、XLSX保存成功；保留的coded MC可读取，关闭的struct字段不存在。
- 额外变量rx_clean成功保存；不存在的missing_var被明确记录，不填造值。
- 原始外部两份MATLAB脚本没有改动；运行副本、源哈希、配置与日志在outputs/matlab_integration_check下。

## 分析核对

读取原验证对应的26信道已有文件，新通用分析中的预测共同噪声、预测实际仿真、网格最小参考、模型训练固定参数四个平均MeanCodedBER，与旧验证统计在1e-12容差内一致。

新版示例输出为outputs/simulation_analysis/workbench_26_final。六张最终PDF的字体和碰撞检查全部通过；逐图检查字体、对数零值单独显示、参数图例位置和颜色一致性。原有验证文件未改写。完整26×294×25仿真本轮没有重新运行。

该通用分析输出描述性比较，未移植旧报告绑定26信道的全部交叉验证、训练重叠审计或固定结论。不存在跨seed结果时不构造误差带。

## 软件检查

本地13项、移植版9项测试通过，包括配置维度检查、8页窗口构建与配置应用、MATLAB脚本只生成副本、任意样本数MAT分析，以及原有训练/单信道/批量预测和进度事件流程。

移植版也携带MATLAB模板与两个依赖函数，不携带本机MATLAB安装或许可证。在新电脑仍需要选择真实matlab.exe并检查其工具箱。模板保留来源路径文字，但工作台执行前会用本次配置替换这些路径。

当前预测模型没有因此重训或替换。配置编辑只更新训练设置，必须另行开始训练才会生成新模型。
