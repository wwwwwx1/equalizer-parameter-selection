# 移植包检查记录

已执行以下检查，均通过：

1. 默认主径501、MATLAB时间626:4000，裁剪后3375×501且主径位于第251列；短记录默认拒绝，显式允许后使用实际末尾。
2. 缺少新电脑数据目录时明确报错。
3. v7.3数值MAT的候选×MC维序恢复及currName字符读取正确。
4. 用三个临时合成样本完整运行：传统MAT配对、打乱CSV的ParamIndex恢复、MeanCodedBER核对、任意样本数全量训练、权重保存/加载、预测、PNG/PDF/SVG出图、禁止将全量模型当保留测试集评估、非空准备目录拒绝覆盖。
5. 预测界面能够构建，主径选项默认501。
6. 代码/配置/启动器已扫描，没有旧电脑用户目录、旧Python路径、旧数据目录或pilot110/coded110/all110工作流依赖。

测试数据与测试权重使用系统临时目录，结束后自动清理。包内data和outputs没有附带旧数据或模型。无需Codex技能目录才能训练、预测或绘图。

这是当前电脑对移植代码的功能检查，不代表已经在目标电脑安装过环境，也不代表已验证目标电脑数据的真实格式、标签对应关系或模型性能。到新电脑后仍需执行环境与数据检查。
