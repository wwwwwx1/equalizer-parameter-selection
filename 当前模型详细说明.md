# 当前模型详细说明

## 1. 本文件描述什么

这份包复制的是当前4层时间Transformer的网络结构，默认从头训练，不含旧模型权重或旧电脑的数据。参数量在默认设置下为342,274个；新电脑尚未训练，因此不能把旧实验的BER当作新模型结果。

任务是给定复数动态信道H与SNR，对有限候选表里的每组(mu,N1,N2)评分，返回最低分对应的组合。监督目标严格为MeanCodedBER。输出分数是归一化性能差距的学习分数，不是校准BER或置信概率。

## 2. 输入、标签与候选表

- 信道：二维时间×时延矩阵`h_vary`，可以是复数。
- SNR：每个信道一个有限dB标量。
- 候选：默认294组三元组，由新数据的param_delt/param_N1/param_N2提取，不自行生成。
- 标签：每个候选的coded_ber_after_mc跨MC列平均，组成长度294的MeanCodedBER向量。
- 训练输入中不包含当前样本的最优参数、BER标签或bestResult。候选表是所有样本共享的查询条件，BER仅用于损失和训练拟合评价。

如果最佳参数并列，全部最优关系保留，不把CSV排序第一行当唯一答案。CSV中的ParamIndex用于恢复原始候选顺序。

## 3. 信道预处理

默认原始主径501，窗口为MATLAB `626:4000,251:751`。完整输入尺寸3375×501，裁后主径为第251列。

`nlms_dfe/data/transforms.py`完成：

1. 按配置恢复时间/时延轴，严格裁剪；可显式允许短时间记录。
2. 计算P=mean(|H|²)，归一化H/sqrt(P)。全零和非有限输入报错。
3. 构造四类特征：Re(H)、Im(H)、|H|、相邻时间相位差angle(H(t)*conj(H(t-1)))。
4. 低能量位置的相位差置零，首行相位差置零。
5. 每16行合并为一个token；普通分支取均值，相位分支用圆周均值再除以π。尾块按真实长度平均。
6. 额外保留log10(P)，与SNR一起作为两个标量特征。

完整输入：3375×501 → 3375×4×501 → 211×4×501。短记录产生不同token数，DataLoader用padding mask标记补齐位置，时间注意力忽略这些位置。

时间池化降低计算量，但会损失快速变化信息。此项是显式近似，配置`temporal_pool=1`可取消；取消后计算与显存明显增加，需要重新训练验证。

## 4. Delay CNN：沿时延提取多径结构

文件：`nlms_dfe/models/encoder.py`，类`DelayCNN`与`ResidualDelayBlock`。

每个时间片共享同一套一维卷积，时间与时延不作对称二维图像处理：

```text
[4,501]
  -> Conv1d(4,32,kernel=5,stride=2,padding=2)
  -> GroupNorm + GELU + 32通道残差块
  -> Conv1d(32,64,kernel=5,stride=2,padding=2)
  -> GroupNorm + GELU + 64通道残差块
  -> Conv1d(64,96,kernel=3,stride=2,padding=1)
  -> GroupNorm + GELU + 96通道残差块
  -> AdaptiveAvgPool1d(8)
  -> 展平为96×8=768维
  -> Linear(768,64)
```

每个残差块有两层kernel=3卷积，输出为GELU(x+body(x))。所以三阶段不表示总共只有三层卷积。保留8个有序时延分箱，避免一个全局平均把主径前后的位置差异全部抹掉。

## 5. 时间Transformer

同文件的`ChannelEncoder`将CNN输出重组为[B,T,64]，添加正弦/余弦位置编码，送入4层独立初始化的TransformerEncoderLayer。

默认设置：d_model=64、heads=4、每头16维、FFN维度128、dropout=0.15、GELU、Pre-LayerNorm。每层保持[B,T,64]形状，通过注意力建模观测窗口内的时间关联。

最后LayerNorm并给每个有效时间token学习一个权重，通过softmax加权求和得到[B,64]信道表示。padding token被设为无权重。

当前不是因果流式Transformer：完整观察窗口共同参与选参，服务离线全窗输入。不能直接认为它能在报文开始前访问未来信道。

## 6. SNR/能量条件化

文件：`nlms_dfe/models/selector.py`，类`ParameterSelector`。

将[SNR,log10(P)]按训练集均值与标准差标准化，经MLP `2→32→64` 得到条件表示。FiLM由这个表示产生两个64维向量gamma与beta：

```text
z_conditioned = z_channel * (1 + gamma) + beta
```

之后拼接条件表示，得到64+64=128维上下文。这里除了SNR还使用了原始信道能量；归一化统计只由新训练数据计算，并随模型保存。

## 7. 候选参数编码与评分

先将mu取log10，N1/N2保持数值，再按固定候选表自身的均值和标准差标准化。候选编码器为 `3→64→32`，带LayerNorm和GELU。

每个候选的32维表示与信道128维上下文拼接：

```text
160 -> Linear 128 -> GELU -> Linear 64 -> GELU -> Linear 1
```

所有候选共享评分器。每批B个信道输出[B,294]，逐行argmin后查回候选表。不能得到网格外的连续新参数。

文件还保留`kind=classifier`分支，可将相同上下文直接映射为294类作对照；默认使用scorer。

## 8. 损失函数

文件：`nlms_dfe/training/losses.py`。

令J_k为第k组的MeanCodedBER，训练目标：

```text
r_k = (J_k - min(J)) / max(P90(J)-P10(J), 0.001)
```

前3轮只用Huber回归。第4轮开始：

```text
L = 1.0 * Huber(score,r)
  + 0.5 * Soft-label cross entropy
  + 1.0 * Expected regret
  + 0.05 * Pairwise ranking
```

软标签q=softmax(-r/0.2)，预测分布p=softmax(-score/0.2)。期望regret为sum(p*r)。排序每次随机取256对候选，差距极小的配对权重更低，并列配对不强制排序。

这些损失同时考虑整个候选性能分布，不等于专门最大化严格Top1，因此平均BER下降时，精确最优命中率不一定上升。第4轮总损失跳变源于新增项，不能把它与前三轮直接作为同一个指标比较。

## 9. 训练策略与保存

入口：`scripts/train_new.py`；核心：`nlms_dfe/training/engine.py`。

默认全部新数据作为train；随机初始化网络，不加载旧权重。AdamW，初始学习率0.0005，weight_decay=0.001，余弦学习率，每批4样本，80轮，梯度裁剪1.0；CUDA时可使用AMP，CPU时关闭AMP。

固定基线是这套训练数据上平均MeanCodedBER最小的一个全局固定候选。每轮日志记录loss、训练拟合BER、regret、Top1/Top3、固定BER、网格最小参考、学习率和耗时。

全量模式下代码变量`val_loader`实际指向训练集，日志前缀为fit，不能解释为验证。配置中保留的patience在全量模式不生效。导出最后一轮，不使用验证早停。

权重文件含state_dict、配置、候选表、候选编号、固定基线编号、epoch和样本快照。标准化统计作为模型buffer保存在state_dict。没有完整优化器和随机状态，所以尚不支持精确断点续训。

## 10. 指标与图

MeanCodedBER为每个信道的候选MC平均，再对信道等权平均。如果新数据每信道比特数/MC次数不同，不能把这个宏平均自动当成所有比特合并后的BER。

Regret=推荐候选真实BER−该信道网格最小BER。Top1允许多个并列最优；Top3表示前三个评分候选中包含至少一个最优。胜平负统计与全局固定参数比较。网格最小值只是有限、有限次MC条件下的经验参考，不是可部署接收机。

训练图展示真实CSV日志：总损失、训练MeanCodedBER、Top1/Top3和regret。全部新数据参与训练，不画不存在的测试曲线，不添加虚构跨seed置信带。

## 11. 文件修改位置

| 想修改的内容 | 文件 |
|---|---|
| 新数据目录、MAT字段、文件名模式 | configs/dataset.json |
| 主径、窗口、特征、时间池化 | configs/train.json的data |
| 层数、宽度、heads、dropout | configs/train.json的model |
| 损失权重、温度、预热轮数 | configs/train.json的loss |
| 轮数、批次、学习率、设备、输出目录 | configs/train.json的training |
| 数据适配新结构 | scripts/prepare_data.py、nlms_dfe/data/io.py |
| CNN/Transformer具体结构 | nlms_dfe/models/encoder.py |
| 条件化和候选评分 | nlms_dfe/models/selector.py |
| 训练循环 | nlms_dfe/training/engine.py |
| 预测窗口 | scripts/predict_gui.py |
| 当前加载的权重 | configs/inference.json，训练成功后自动更新 |

## 12. 适用边界

该网络学习有限网格在给定信道、SNR和仿真协议下的性能关系，不保证逐信道最优或新信道必胜固定参数。更大网络也不自动解决标签噪声、样本不够、时间池化过强或输入与接收链不一致的问题。

新电脑数据训练完成后，应该用未参与训练的新信道、对应SNR与相同协议，独立仿真推荐参数和固定参数。Python的simulation.py仅保留接口，未替代你的MATLAB均衡器。旧电脑的实验数值不作为这份新训练的性能承诺。
